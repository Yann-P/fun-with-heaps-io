# Experiments with `hxd.WaitEvent` and h2d.`Console`


## Usage:

type `/` to spawn the console

###  `/start {timerid} {word}`

repeats word n times every second

### `/stop {timerid}`

stops a timer
